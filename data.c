#include "data.h"

bool create_file(char* name) {
    // int fd = open(name, O_CREAT | O_WRONLY, 0644);
    // if (fd == -1) {
    //     return false;
    // }
    // close(fd);
    // return true;
}